# Root Instructions

Use the repository policy broker configuration.

- Prefer focused policy changes.
- Keep generated bundles concise.

This repository has several examples and nested fixture directories that explain how instruction
discovery is expected to behave across different source types, but that background should not be
treated as hard task guidance.

```bash
cargo test
```
