id: example.monorepo.api
version: 1
status: active
owner: example-api
priority: 70
applies_when:
  paths:
    - packages/api/**
  languages:
    - rust
  task_types:
    - fix_bug
    - add_feature
instructions:
  - Return `Result` from fallible helpers instead of panicking.
  - Add focused unit tests for API behavior changes.
required_checks:
  - monorepo.api.tests
