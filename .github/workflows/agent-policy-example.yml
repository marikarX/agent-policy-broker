# Example only: copy or adapt this workflow before enabling it in a production repo.
name: Agent Policy PR Example

on:
  pull_request:

permissions:
  contents: read
  pull-requests: read

env:
  # Set this to "true" in your copied workflow if policy lookup failures should
  # block pull requests.
  AGENT_POLICY_FAIL_ON_REPORT: "false"

jobs:
  agent-policy:
    name: Agent Policy example report
    runs-on: ubuntu-latest

    steps:
      - name: Check out trusted agent-policy source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: agent-policy-source
          persist-credentials: false

      - name: Check out pull request for analysis
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          path: pull-request
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build trusted agent-policy
        run: cargo build --manifest-path agent-policy-source/Cargo.toml --release -p agent-policy-cli

      - name: Collect changed files
        id: changed-files
        shell: bash
        run: |
          set -euo pipefail

          git -C pull-request diff --name-only --diff-filter=ACMR \
            "${{ github.event.pull_request.base.sha }}" HEAD > changed-files.txt

          {
            echo "count=$(wc -l < changed-files.txt | tr -d ' ')"
            echo "path=changed-files.txt"
          } >> "$GITHUB_OUTPUT"

      - name: Run agent-policy example report
        id: agent-policy
        shell: bash
        run: |
          set +e

          binary="agent-policy-source/target/release/agent-policy"
          report="agent-policy-report.md"
          task="Review pull request changes"

          mapfile -t changed_files < changed-files.txt

          if [ "${#changed_files[@]}" -gt 0 ]; then
            "$binary" get \
              --repo pull-request \
              --task "$task" \
              --type review \
              --files "${changed_files[@]}" \
              --format markdown > "$report" 2> agent-policy-error.log
          else
            "$binary" inspect \
              --repo pull-request \
              --format markdown > "$report" 2> agent-policy-error.log
          fi

          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"

          {
            echo "# Agent Policy PR Example"
            echo
            echo "This is an example summary generated by \`agent-policy\`."
            echo
            echo "Changed files: ${#changed_files[@]}"
            echo
            if [ "${#changed_files[@]}" -gt 0 ]; then
              echo "<details><summary>Changed file list</summary>"
              echo
              sed 's/^/- `/' changed-files.txt | sed 's/$/`/'
              echo
              echo "</details>"
              echo
            fi

            if [ "$status" -eq 0 ]; then
              cat "$report"
            else
              echo "Agent Policy exited with status \`$status\`."
              echo
              echo '```text'
              cat agent-policy-error.log
              echo '```'
            fi
          } >> "$GITHUB_STEP_SUMMARY"

          if [ "$AGENT_POLICY_FAIL_ON_REPORT" = "true" ] && [ "$status" -ne 0 ]; then
            exit "$status"
          fi

